## 🎨 Static Files

The `static/` folder contains all the styling and visual assets used in the project. It helps improve the user interface and overall look of the application.

---

### 📁 Contents
- **CSS Files**  
  - Used for styling the web pages (colors, layout, fonts, responsiveness)
    
---

### ⚙️ Usage
- Static files are linked in HTML templates using Flask’s `url_for()` method:
  
